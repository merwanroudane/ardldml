% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/firststage.R
\name{cross_fit_projection}
\alias{cross_fit_projection}
\title{Cross-Fitted Projection}
\usage{
cross_fit_projection(
  X,
  y,
  folds,
  lam = NULL,
  adaptive = TRUE,
  c = 1.1,
  penalised = TRUE,
  adaptive_mask = NULL
)
}
\arguments{
\item{X}{Numeric matrix of regressors.}

\item{y}{Numeric target.}

\item{folds}{An \code{"ardl_folds"} object from \code{\link{hblock_folds}}.}

\item{lam}{Penalty passed to \code{\link{adaptive_post_lasso}}.}

\item{adaptive}{Adaptive weighting.}

\item{c}{Constant in the plug-in penalty.}

\item{penalised}{If \code{FALSE}, the projection is unpenalised least
squares: the low-dimensional corner, and the \code{"ols"} arm of the
trend-absorption diagnostic.}

\item{adaptive_mask}{Columns receiving adaptive weights.}
}
\value{
A list with \code{fitted}, \code{resid}, \code{support_union},
  \code{lams} and \code{estimable}.
}
\description{
Out-of-fold predictions of \code{y} on \code{X} under an h-block partition.
Each evaluation block is predicted by a model estimated on that block's
buffered training set, so the first-stage error is decoupled from the
evaluation-fold innovations.
}
\examples{
set.seed(1)
X <- matrix(rnorm(80 * 3), 80, 3)
y <- X[, 1] + rnorm(80)
fit <- cross_fit_projection(X, y, hblock_folds(80, 4, 2), penalised = FALSE)
stats::sd(fit$resid)

}
