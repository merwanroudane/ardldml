% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistic.R
\name{dml_spec}
\alias{dml_spec}
\alias{print.dml_spec}
\title{Specification of a DML-Bounds Fit}
\usage{
dml_spec(
  lags = 4L,
  case = 3L,
  n_blocks = 5L,
  buffer = 0L,
  adaptive = TRUE,
  penalised = TRUE,
  penalty = "plugin",
  c = 1.1,
  integrated = NULL,
  include_constant = FALSE,
  adaptive_integrated_only = TRUE,
  dlags = FALSE
)

\method{print}{dml_spec}(x, ...)
}
\arguments{
\item{lags}{Short-run lag order \eqn{p}.}

\item{case}{Deterministic case, 1 or 3. This does \strong{not} enter the
statistic, which is a no-intercept projection on the orthogonalised levels
regardless. It sets the deterministic terms of the restricted conditional
model the bootstrap resamples from.}

\item{n_blocks}{Cross-fitting blocks \eqn{K}.}

\item{buffer}{Cross-fitting buffer \eqn{h}.}

\item{adaptive}{Adaptive weights on the level projection. \code{TRUE} is the
paper's default; \code{FALSE} gives the plain-LASSO arm of the diagnostic.}

\item{penalised}{If \code{FALSE}, both projections are unpenalised least
squares -- the low-dimensional corner and the \code{"ols"} arm.}

\item{penalty}{How the \eqn{\Delta Y} penalty is chosen: \code{"plugin"} (the
default), one of \code{"low"}, \code{"medium"}, \code{"high"} (equivalently
\code{"min"}, \code{"mid"}, \code{"1se"}) for rolling-origin
cross-validation, or a number fixing it directly. The level projection
always uses the plug-in rule.}

\item{c}{Constant in the plug-in penalty.}

\item{integrated}{Character vector of controls to treat as integrated of
order one.}

\item{include_constant}{Add an intercept to the final regression. Defaults to
\code{FALSE}, which is the paper's equation (10) as written; the first-stage
projections already carry intercepts, so the residuals are mean-zero by
construction. Exposed only for sensitivity checks.}

\item{adaptive_integrated_only}{Restrict the adaptive weights to the
integrated block, which is what the paper specifies.}

\item{dlags}{Include lags of \eqn{\Delta D} in the conditional design.}

\item{x}{A \code{"dml_spec"} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"dml_spec"}.
}
\description{
Everything that defines a fit, held separately from the data so the bootstrap
can rebuild the identical statistic on a regenerated path.
}
\examples{
dml_spec(lags = 4, n_blocks = 5, buffer = 6)
}
