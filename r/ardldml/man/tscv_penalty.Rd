% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/firststage.R
\name{tscv_penalty}
\alias{tscv_penalty}
\title{Rolling-Origin Cross-Validation for the Penalty}
\usage{
tscv_penalty(
  X,
  y,
  grid = NULL,
  min_train = NULL,
  adaptive = FALSE,
  n_grid = 20L,
  rule = "min",
  return_profile = FALSE
)
}
\arguments{
\item{X}{Numeric matrix of regressors.}

\item{y}{Numeric target.}

\item{grid}{Penalties to search. Defaults to a log grid spanning two decades
around the plug-in value.}

\item{min_train}{\eqn{T_0}. Defaults to \code{max(20, n \%/\% 3)}.}

\item{adaptive}{Adaptive weights during the search.}

\item{n_grid}{Grid size when \code{grid} is \code{NULL}.}

\item{rule}{One of \code{"min"} (Low), \code{"1se"} (High) or \code{"mid"}
(Medium, the geometric midpoint); the labels \code{"low"}, \code{"medium"}
and \code{"high"} are accepted too.}

\item{return_profile}{Also return the grid and its error profile.}
}
\value{
A single penalty, or a list with the profile if
  \code{return_profile = TRUE}.
}
\description{
For origins \eqn{t = T_0, \ldots, T-1} the first stage is fitted on
\eqn{\{1, \ldots, t\}} and evaluated one step ahead, and \eqn{\lambda}
minimises the average out-of-sample squared error. This respects temporal
ordering, unlike ordinary k-fold cross-validation, which would train on the
future.
}
\details{
The verdict can depend on which point of the profile is taken, which is why
\code{\link{penalty_sensitivity}} sweeps all three rather than reporting one.
}
\examples{
set.seed(1)
X <- matrix(rnorm(60 * 4), 60, 4)
y <- X[, 1] + rnorm(60)
tscv_penalty(X, y, n_grid = 4, min_train = 45)

}
